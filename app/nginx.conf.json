{
  "listeners": {
    "*:80": {
      "pass": "routes"
    }
  },

  "routes": [
    {
      "action": {
        "share": "/www/static/$uri"
      }
    }
  ],

  "access_log": {
    "path": "/var/log/access.log"
  }
}
